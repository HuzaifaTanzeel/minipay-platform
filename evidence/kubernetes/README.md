# Evidence from kubectl

Put redacted `get` / `describe` / `endpoints` / logs here after you apply the working overlay and, separately, the v0 manifest in `minipay-legacy`. See [kubernetes/RUNBOOK.md](../../kubernetes/RUNBOOK.md).
