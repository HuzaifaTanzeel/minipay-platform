[pytest]
# Rootdir stays the repository so evidence paths and imports are stable.
# Invoke a suite by path: tests/api, tests/ui, python/tests.
# Do not set testpaths — a bare `pytest` would launch Chromium for UI tests.
markers =
    ui: browser journeys against the Compose web UI
